config EMPTY_SLOT
    bool

config PTIMER
    bool

config DEVICE_TREE
    bool
    depends on FDT

config FITLOADER
    bool
    depends on DEVICE_TREE

config GENERIC_LOADER
    bool
    default y

config OR_IRQ
    bool

config PLATFORM_BUS
    bool
    depends on DEVICE_TREE

config REGISTER
    bool

config SPLIT_IRQ
    bool

config EIF
    bool
    depends on LIBCBOR && GNUTLS
