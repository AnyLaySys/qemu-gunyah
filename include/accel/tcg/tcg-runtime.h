/*
 * Stub header kept for non-TCG builds.
 *
 * Some common helper generation headers include this path even when the TCG
 * accelerator is disabled.  The runtime implementations are intentionally
 * removed from this tree.
 */
